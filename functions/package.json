{
  "name": "nexthire-email-functions",
  "private": true,
  "version": "1.0.0",
  "description": "NextHire Firebase Cloud Functions for transactional email notifications",
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "clean": "tsc --build --clean",
    "serve": "npm run build && firebase emulators:start --only functions,firestore",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0",
    "resend": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
